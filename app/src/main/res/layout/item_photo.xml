<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/size_item"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="@dimen/margin_item"
    android:paddingLeft="@dimen/margin_item"
    android:paddingRight="@dimen/margin_item"
    android:orientation="vertical">

    <com.makeramen.roundedimageview.RoundedImageView
        android:id="@+id/image"
        app:riv_corner_radius="@dimen/raduis_corner_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:ignore="ContentDescription"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/overlay_image"/>

    <TextView
        android:id="@+id/description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center|bottom"
        android:textAllCaps="true"
        android:lineSpacingExtra="@dimen/line_spacing_desc"
        android:paddingLeft="@dimen/padding_item_desc"
        android:paddingRight="@dimen/padding_item_desc"
        android:textColor="@color/colorAccent"/>

</FrameLayout>
